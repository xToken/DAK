//afkkick default config

kDAKRevisions["pause"] = "0.1.118a"

local function SetupDefaultConfig()
	//kDAKConfig.Pause = { }
end

table.insert(kDAKPluginDefaultConfigs, {PluginName = "pause", DefaultConfig = SetupDefaultConfig })